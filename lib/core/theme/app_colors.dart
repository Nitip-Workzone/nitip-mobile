import 'package:flutter/material.dart';

class AppColors {
  // ── Penitip (Primary: Indigo) ─────────────────────────
  static const Color primary        = Color(0xFF4F46E5); // indigo-600
  static const Color primaryDark    = Color(0xFF3730A3); // indigo-800
  static const Color primaryMid     = Color(0xFF6366F1); // indigo-500
  static const Color primaryLight   = Color(0xFFEEF2FF); // indigo-50

  // ── Runner (Secondary: Indigo - Sama dengan Penitip) ──
  static const Color secondary      = Color(0xFF4F46E5); // indigo-600
  static const Color secondaryDark  = Color(0xFF3730A3); // indigo-800
  static const Color secondaryMid   = Color(0xFF6366F1); // indigo-500
  static const Color secondaryLight = Color(0xFFEEF2FF); // indigo-50

  // ── Background Colors ─────────────────────────────────
  static const Color background = Colors.white;
  static const Color surface    = Color(0xFFF8FAFC);

  // ── Text Colors ───────────────────────────────────────
  static const Color textMain  = Color(0xFF0F172A);
  static const Color textMuted = Color(0xFF64748B);

  // ── Semantic Colors ───────────────────────────────────
  static const Color success = Color(0xFF10B981);
  static const Color error   = Color(0xFFEF4444);
  static const Color warning = Color(0xFFF59E0B);

  // ── Decorative ────────────────────────────────────────
  static const Color glassWhite = Color(0x1AFFFFFF);
  static const Color border     = Color(0xFFE2E8F0);
}
